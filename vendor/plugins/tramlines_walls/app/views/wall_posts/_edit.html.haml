#edit_wall_post_form
  -semantic_remote_form_for wall_post do |form|
    =hidden_field_tag :in_moderation, params[:in_moderation]
    -form.inputs do
      =form.input :text, :as => :text, :input_html => {:rows => 4}, :label => false
    -form.buttons do
      =form.commit_button "Save"