- semantic_form_for @blog, :html => {:multipart => true} do |form|
  -form.inputs do
    =form.input :name
    =form.input :description, :input_html  => {:rows => 5}
    =form.input :image, :as => :file
    =form.input :member_id, :as => :hidden
  - form.buttons do 
    = form.commit_button