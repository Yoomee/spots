-title "Activities"
%ul.lst
  -@activities.each do |activity|
    %li
      =link_to_self(activity)

-options_panel do
  =back_link
  =link_if_allowed 'New activity', new_activity_proc
  =link_if_allowed 'Set order', order_activities_proc