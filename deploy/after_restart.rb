run "cd #{current_path} && rake ts:rebuild RAILS_ENV=#{environment} -t"

# Workling
# # Start new workling client
# puts "Starting workling"
# run "cd #{release_path} && sudo RAILS_ENV=#{environment} script/workling_client start"