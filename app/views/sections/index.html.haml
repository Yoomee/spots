-title 'Sections'
-if @sections.empty?
  %p
    There are no sections yet.
-else
  =render :partial => 'children', :locals => {:children => @sections}
-options_panel do
  =back_link
  =link_if_allowed 'New section', new_section_proc
  =link_if_allowed 'Order root sections', order_sections_proc
  =link_if_allowed "XML sitemap", sections_proc(:format => :xml), :target => "_blank"