- title @mailing.name

%p
  %strong From:
  =h @mailing.from
%p
  %strong Subject:
  =h @mailing.subject
%p
  %strong Body:
  %br
  =@mailing.html_body
%p
  =link_if_allowed("Send", send_emails_mailing_proc(@mailing), :class => "bttn_l", :confirm => "Are you sure?") if @mailing.not_sent?
-options_panel do
  =back_link
  =link_if_allowed "All emails", mailings_proc
  =link_if_allowed "View recipients", mailing_mails_proc(@mailing)
  =link_if_allowed "Edit", edit_mailing_proc(@mailing)
  =link_if_allowed "Delete", @mailing, :method => :delete, :confirm => "Are you sure?"